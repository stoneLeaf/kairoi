<h1>Edit project <%= @project.name %></h1>
<%= form_for(@project, url: project_path(@project.to_param)) do |f| %>
  <%= form_errors_for @project %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Update", class: "btn btn-primary" %>
<% end %>
